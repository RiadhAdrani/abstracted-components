class Outline {
     static new({ width, style, color, offset }) {
          return {
               width: `${width}`,
               style: `${style}`,
               color: `${color}`,
               offset: `${offset}`,
          };
     }
}

export default Outline;
