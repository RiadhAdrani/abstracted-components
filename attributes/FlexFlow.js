class FlexFlow {
     static new(flexDirection, flexWrap) {
          return {
               direction: `${flexDirection}`,
               wrap: `${flexWrap}`,
          };
     }
}

export default FlexFlow;
